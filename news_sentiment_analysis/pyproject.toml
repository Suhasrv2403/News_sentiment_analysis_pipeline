[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# Ruff's default set (pyflakes + a pycodestyle subset). Explicit here so CI
# lint results don't depend on whatever ruff config happens to be picked up
# from outside this project.
select = ["E4", "E7", "E9", "F"]
