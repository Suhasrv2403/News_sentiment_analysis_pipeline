[pytest]
# Makes `include.*` and `dags.*` importable as top-level packages when
# pytest is invoked from this directory (news_sentiment_analysis/), which
# is how the Astro CLI's own test runner and CI both run it.
pythonpath = .
testpaths = tests
